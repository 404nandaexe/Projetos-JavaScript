// Selecionando elementos HTML

// Display da calculadora
const result = window.document.getElementById('result')

// Botôes numéricos
const numbers = window.document.getElementsByClassName('number')

// Botôes operadores
const operators = window.document.getElementsByClassName('operator')

// Botôes especiais
const equal = window.document.getElementById('equal')
const clear = window.document.getElementById('clear')
const backspace = window.document.getElementById('backpace')

// Estado da calculadora

// Número que está 